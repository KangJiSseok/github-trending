# GIT-7 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add a package-owned Prisma/PostgreSQL foundation under `packages/db` with a local Docker runtime and the minimum schema needed for later ingestion and aggregation issues.

**Architecture:** Mirror the existing `packages/config` shared-package shape, but let `packages/db` own Prisma schema, generated client output, tests, and DB scripts. Keep root changes thin and operational: env example, Docker Compose, README instructions, and optional root helper scripts.

**Tech Stack:** TypeScript, pnpm workspace, Prisma, PostgreSQL, Docker Compose, Vitest

---

### Task 1: Create the DB package test harness

**Files:**
- Create: `packages/db/package.json`
- Create: `packages/db/tsconfig.json`
- Create: `packages/db/vitest.config.ts`
- Create: `packages/db/src/client.test.ts`

**Step 1: Create the package manifest and test config**

Add a new shared package named `@repo/db` with `build`, `test`, `lint`, `db:generate`, and `db:migrate` scripts.

**Step 2: Install the new package dependencies**

Run: `pnpm install`
Expected: lockfile updates to include Prisma, PostgreSQL-adjacent package dependencies, and the new workspace package.

**Step 3: Write the failing test**

Create `packages/db/src/client.test.ts` asserting that `createPrismaClient()` returns a defined Prisma client instance.

**Step 4: Run the focused package test**

Run: `pnpm --filter @repo/db test`
Expected: fail because `./client` does not exist yet

### Task 2: Implement the Prisma package boundary

**Files:**
- Create: `packages/db/prisma.config.ts`
- Create: `packages/db/src/client.ts`
- Create: `packages/db/src/index.ts`

**Step 1: Add the minimal package exports**

- Configure Prisma to use `prisma/schema.prisma`
- Export `createPrismaClient()` from `src/client.ts`
- Re-export package surface from `src/index.ts`

**Step 2: Re-run the focused package test**

Run: `pnpm --filter @repo/db test`
Expected: pass

### Task 3: Add the initial Prisma schema

**Files:**
- Create: `packages/db/prisma/schema.prisma`

**Step 1: Define the database models**

Add the seven issue-scoped tables with primary keys, timestamps, foreign keys, and minimum uniqueness constraints needed for ingestion/idempotency.

**Step 2: Generate Prisma client artifacts**

Run: `pnpm --filter @repo/db db:generate`
Expected: generated client appears under `packages/db/generated/client`

### Task 4: Add local DB runtime files

**Files:**
- Create: `.env.example`
- Create: `docker-compose.yml`
- Modify: `README.md`
- Modify: `package.json`

**Step 1: Add local database runtime config**

- `.env.example` defines `DATABASE_URL` and `DIRECT_URL`
- `docker-compose.yml` adds one PostgreSQL service with healthcheck
- `README.md` documents DB startup and migration flow
- `package.json` may add thin root helper scripts that wrap the DB package

**Step 2: Bootstrap the real local environment file**

Create a root `.env` by copying `.env.example` and using the local PostgreSQL connection values expected by Prisma.

**Step 3: Start the local database**

Run: `docker compose up -d`
Expected: PostgreSQL container becomes healthy

### Task 5: Verify migrations and package health

**Files:**
- No new files expected

**Step 1: Apply the development migration**

Run: `pnpm --filter @repo/db db:migrate`
Expected: local schema is created successfully

**Step 2: Re-run generation and tests**

Run:

```bash
pnpm --filter @repo/db db:generate
pnpm --filter @repo/db test
```

Expected: both pass

**Step 3: Run workspace validation**

Run:

```bash
pnpm build
pnpm test
```

Expected: the new DB package participates cleanly in recursive validation

### Notes

- Do not integrate `@repo/db` into `apps/web` or `apps/worker` in this issue.
- Keep the first test/package API minimal; later issues can expand query helpers and test database setup.
- Do not commit unless the user explicitly asks for a commit.
